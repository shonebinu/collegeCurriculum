#include <stdio.h>

int binarySearch(int x[], int size, int key) {
    int L, R, mid;
    
    while ()
}

int main() {
    int array[5] = {1, 4, 2, 5, 9};
    printf("Location: %d\n", linearSearch(array, 5, 5));
    return 0;
}